spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/auth
    username: auth
    password: auth
    driver-class-name: org.mariadb.jdbc.Driver
  mail:
    username: ${MAIL_USERNAME:allocmoirai@gmail.com}
    password: ${MAIL_PASSWORD:uejo qpli woth fdru}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

jwt:
  secret: oTIu0wcG74gvm+AtU907bamJK3EraoAgcAia09SmWIZgsKUFipeU12DmPawCVPO8FZUf4PJGL5fXWrNDoVG4uQ== # ?? 256bit Base64
  access-exp: 500                     # 8?(?)
  refresh-exp: 12000               # 14?(?)

mail:
  from: ${MAIL_FROM:allocmoirai@gmail.com}
